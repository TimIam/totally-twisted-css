// Local preview only. Production serves the three static app files directly.
const http=require('node:http'),fs=require('node:fs'),path=require('node:path');
const root=__dirname;const types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.png':'image/png'};
http.createServer((req,res)=>{let file;try{const url=new URL(req.url,'http://localhost');let pathname=decodeURIComponent(url.pathname).replace(/^\/totallytwisted(?=\/|$)/,'');if(pathname==='/'||pathname==='')pathname='/index.html';file=path.resolve(root,'.'+pathname);if(!file.startsWith(root+path.sep)||!['index.html','styles.css','app.js','upstream.css','artwork.css','assets/twisted-sprites.png'].includes(path.relative(root,file).replaceAll(path.sep,'/'))){res.writeHead(404);res.end();return}}catch{res.writeHead(400);res.end();return}fs.readFile(file,(err,data)=>{res.writeHead(err?404:200,{'Content-Type':types[path.extname(file)]||'text/plain','Cache-Control':'no-store'});res.end(err?'Not found':data)})}).listen(4173,'127.0.0.1',()=>console.log('Preview: http://127.0.0.1:4173'));
